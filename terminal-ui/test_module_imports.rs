// Test to import the modules correctly
extern crate terminal_ui;
use terminal_ui::layout::manager::LayoutManager;
use terminal_ui::layout::pane::{PaneManager, SplitOrientation};
use terminal_ui::layout::tab::TabManager;

fn main() {
    println!("Successfully imported all the required modules!");
}